nelmio_api_doc:
    documentation:
        info:
            title: Kuru's Airsofts API
            description: API REST pour la plateforme e-commerce Kuru's Airsofts
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc$)
